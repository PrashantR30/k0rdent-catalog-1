apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "llama-stack.fullname" . }}
  labels:
    {{- include "llama-stack.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "llama-stack.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "llama-stack.labels" . | nindent 8 }}
    spec:
      containers:
        - name: llama-stack
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # distribution-starter's entrypoint accepts CLI args directly
          command: ["/bin/sh","-lc"]
          args:
            - >
              exec llamastack/distribution-starter
{{- range .Values.extraArgs }}
              {{ . }}
{{- end }}
          ports:
            - containerPort: {{ .Values.service.port }}
              name: http
          env:
{{- if .Values.env }}
{{- toYaml .Values.env | nindent 12 }}
{{- else }}
            []
{{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
{{- toYaml .Values.resources | nindent 12 }}
{{- if and .Values.config.enabled .Values.config.raw }}
          volumeMounts:
            - name: config
              mountPath: /config
{{- end }}
      volumes:
{{- if and .Values.config.enabled .Values.config.raw }}
        - name: config
          configMap:
            name: {{ include "llama-stack.fullname" . }}-config
            items:
              - key: llama_stack.yaml
                path: llama_stack.yaml
{{- end }}
